image:
  # name: ghcr.io/viperproject/gobra:latest
  name: ghcr.io/viperproject/gobra@sha256:0578468fcef0a6f347126253ce535116f55308d513f68ff788353b87c52ae812 # Gobra commit 1bc4960
  # override entrypoint such that we get a shell in the image
  # as the image would otherwise expect a path to a Gobra file
  entrypoint: [""]

verify:
  script:
    - ./verify.sh
  artifacts:
    when: always
    paths:
      - .gobra/stats.json
