image:
  # name: ghcr.io/viperproject/gobra:latest
  name: ghcr.io/viperproject/gobra@sha256:f2c12b310d5d595161c04b4f3b559166029c67bfba0a38bdea27699d436803e3
  # override entrypoint such that we get a shell in the image
  # as the image would otherwise expect a path to a Gobra file
  entrypoint: [""]

verify:
  script:
    - ./verify.sh
  artifacts:
    when: always
    paths:
      - ./.gobra/stats.json
